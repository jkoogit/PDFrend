{
  "name": "pdfrend-monorepo",
  "private": true,
  "scripts": {
    "backend:dev": "pnpm --filter backend dev",
    "backend:build": "pnpm --filter backend build",
    "backend:db:generate": "pnpm --filter backend db:generate",
    "backend:db:push": "pnpm --filter backend db:push"
  },
  "devDependencies": {
    "pnpm": "^9.0.0"
  }
}
