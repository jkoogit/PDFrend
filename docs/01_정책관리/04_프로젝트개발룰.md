# 04. 프로젝트 개발 룰 (Development Rules)

## 개요
매 작업 단계에서 코드 품질과 관리 체계를 유지하기 위해 반드시 준수해야 할 필수 요건입니다.

## 1. Git & PR 워크플로우
- [ ] 모든 작업은 GitHub Issue 생성 후 시작한다.
- [ ] 이슈 타이틀에는 반드시 이슈 번호를 포함한다 (예: `[이슈 #2] DB 스키마 설계`).
- [ ] 작업 브랜치는 `main`에서 생성하며 `feature/#이슈번호-작업명` 형식을 사용한다.
- [ ] 작업 완료 후 `dev` 브랜치로 **Pull Request(PR)**를 생성한다.
- [ ] PR 내에서 Manus의 **셀프 코멘트**와 사용자의 리뷰를 거친 후, **사용자의 명시적인 머지 지시가 있을 때 Manus가 머지를 수행**한다.
- [ ] 다음 작업 시작 전, 이전 작업 소스를 `stg` 및 `main`으로 승격시킨다.

## 2. 문서화 및 학습 (Hybrid Review)
- [ ] 새로운 기술 결정이나 정책 변경 시 관련 문서를 즉시 업데이트한다.
- [ ] 이슈 발생 시 `{YYMMDD}_{순번}_{문제}_{원인}.md` 형식으로 리포트를 작성한다.
- [ ] 작업 완료 후 **Deep Dive 보고서**와 **심화 학습 가이드**를 작성한다.
- [ ] 리뷰 보고서 파일명 규칙(`날짜_순번_리뷰구분_#이슈번호_이슈제목`)을 준수한다.

## 3. 테스트 및 검증 프로세스
- [ ] **작업 전 설계**: 이슈 등록 후 개발 시작 전, `docs/10_테스트관리`에 기대 결과와 테스트 계획(단위/통합/인수)을 먼저 작성한다.
- [ ] **테스트 우선 개발(TDD) 검토**: 작업 리뷰 단계에서 테스트 우선 개발이 필요한지 검토하고, 필요 시 테스트 코드를 먼저 작성한다.
- [ ] **검증 수행**: 기능 구현 후 설계된 테스트 항목에 따라 검증을 수행하고 결과를 기록한다.
- [ ] 각 폴더의 진입점 파일은 `index_{역할}.ts` 형식을 사용한다. (예: `index_svr.ts`, `index_db.ts`, `index_sch.ts`)
- [ ] `npx tsc --noEmit`을 통해 타입 오류가 없음을 확인한다.
- [ ] ESLint를 실행하여 린트 에러가 없음을 확인한다.
- [ ] Cloudtype 배포 후 실제 URL에서 정상 동작 여부를 최종 확인한다.

## 4. 환경 및 보안
- [ ] 민감 정보(비밀번호, API 키)는 절대 코드나 문서에 직접 기록하지 않는다.
- [ ] 환경 변수는 `docs/01_정책관리/03_환경변수관리대장.md`에 항목만 등록하여 관리한다.
- [ ] 모든 브랜치(`dev`, `stg`, `main`)의 동기화 상태를 주기적으로 점검한다.

## 5. 문서 작성 이력 관리
- [ ] 진행되는 모든 문서는 파일 **마지막 섹션**에 `작성 이력`을 유지한다.
- [ ] 작성 이력은 아래 3개 항목을 필수로 기록한다.
  - 작업일시 (UTC 또는 KST 명시)
  - 작업 에이전트 (예: Codex, Manus, Human)
  - 내용 한 줄 요약
- [ ] 한 이슈가 종료되면 다음 채팅/세션에서 이어갈 수 있도록, 마지막 항목에 "다음 세션 시작 포인트"를 남긴다.
- [ ] 이 규칙은 정책/기획/설계/기술/테스트/리뷰/이슈 문서 전체에 공통 적용한다.

## 6. 작업 표준 흐름 (GBRD 기반)
1. **초기작업**
   1.1 이슈 등록
   1.2 작업브랜치 등록
   1.3 작업대상 분석
   1.4 실행계획 수립
   1.5 완료기준 수립
2. **피드백 검토**
   2.1 작업대상/실행계획/완료기준 타당성 점검
   2.2 설계/기술 검토 및 의사결정 상호검증
3. **작업 및 리뷰**
   3.1 린트(점검현황 보고서)
   3.2 리뷰(완료기준 대비 작업현황 보고서)
   3.3 심층(피드백 대응결과 보고서)
4. **레포 반영**: 이슈 브랜치 -> `dev` 머지
5. **개발 서비스 반영**: Cloudtype 배포
6. **서비스 테스트**: 배포 이슈 정리
7. **PR 리뷰**
8. **회고리뷰/작업 종료 문서화/다음 채팅 인계 정리**
9. **격려 및 채팅 작업종료 선언**

### 작성 이력
| 작업일시 | 작업 에이전트 | 내용 한 줄 요약 |
| :--- | :--- | :--- |
| 2026-05-13 00:00 UTC | Codex (GPT-5.3-Codex) | 문서 말미 작성 이력 필수 규칙(작업일시/에이전트/요약) 추가 및 세션 인계 원칙 반영 |
| 2026-05-13 00:00 UTC | Codex (GPT-5.3-Codex) | GBRD 기반 작업 표준 흐름(초기작업~종료)을 정책으로 현행화 |
