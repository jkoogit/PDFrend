applications:
  - name: pdfrend-backend-dev
    project: pdfrend
    type: nodejs
    context:
      git:
        url: https://github.com/jkoogit/PDFrend.git
        ref: dev
      root: apps/backend
    spec:
      runtime: nodejs-20
      build: pnpm install && pnpm run build
      start: npm start
      port: 4000
      env:
        - name: NODE_ENV
          value: production
        - name: ENVIRONMENT
          value: dev
        - name: PORT
          value: "4000"
